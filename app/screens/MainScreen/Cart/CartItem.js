import React from "react";
import { Text, View } from "react-native";

const CartItem = ({ item }) => {
  return (
    <View>
      <Text>CartItem</Text>
    </View>
  );
};

export default CartItem;
