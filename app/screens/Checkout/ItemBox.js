import React from "react";
import { View, Text } from "react-native";

const ItemBox = ({ productItems = [] }) => {
  return (
    <View>
      <Text>Items</Text>
    </View>
  );
};

export default ItemBox;
